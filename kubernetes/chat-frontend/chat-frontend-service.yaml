apiVersion: v1
kind: Service
metadata:
  name: chat-frontend-service
  namespace: tema-std
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.0.0.102
  labels:
    app: chat-frontend
spec:
  selector:
    app: chat-frontend
  ports:
    - port: 90
      targetPort: 90
  type: LoadBalancer
