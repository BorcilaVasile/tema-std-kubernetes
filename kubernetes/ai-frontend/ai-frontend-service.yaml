apiVersion: v1
kind: Service
metadata:
  name: ai-frontend-service
  namespace: tema-std
  labels:
    app: ai-frontend
spec:
  selector:
    app: ai-frontend
  ports:
    - port: 100
      targetPort: 80
  type: ClusterIP
